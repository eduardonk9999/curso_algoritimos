let number1 = parseInt(prompt("Digite o primeiro número"));
let number2 = parseInt(prompt("Digite o segundo número"));

function soma(numUm, numDois){
  let somaNumeros = numUm + numDois;
  return somaNumeros;
}

soma(1, 2);